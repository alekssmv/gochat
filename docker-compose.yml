version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8080:8080
  nginx:
    image: nginx:latest
    ports:
      - 81:81
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf